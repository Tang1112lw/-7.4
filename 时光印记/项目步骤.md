"""
# ============================================
# 时光印记 · 项目真实进度（2026-06-30 同步）
# ============================================

## 环境准备 ✅ 已完成
1. 创建虚拟环境 `venv/`
2. 安装依赖：
   ```
   pip install fastapi uvicorn jinja2 python-multipart sqlalchemy aiomysql passlib openpyxl
   ```

## 项目架构
时光印记/
├── venv/                          # 虚拟环境（已存在）
├── app/                           # 所有代码放这里
│   ├── __init__.py
│   ├── main.py                    # ❌ 空文件 — 项目还跑不起来
│   ├── config.py                  # ✅ 配置（⚠️ 数据库名是 students，规划是 timeprint）
│   ├── databases.py               # ✅ 引擎+会话工厂（⚠️ 缺少公共 get_db 函数）
│   ├── models/
│   │   ├── __init__.py            # ✅ Base 基类
│   │   ├── user.py                # ✅ 用户模型（username + password）
│   │   ├── member.py              # ✅ 家庭成员模型（name + birthday + user_id）
│   │   └── story.py               # ✅ 成长故事模型（title + content + time + member_id）
│   ├── routers/
│   │   ├── __init__.py            # ❌ 空文件
│   │   ├── auth.py                # ✅ 注册/登录（⚠️ 缺少 from ..databases import SessionLocal）
│   │   ├── member.py              # ⚠️ 只写了列表查询，缺增/删/改
│   │   └── story.py               # ❌ 空文件
│   ├── services/
│   │   ├── __init__.py            # ❌ 空文件
│   │   ├── auth.py                # ✅ 密码加密+校验
│   │   └── export.py              # ❌ 空文件
│   └── templates/
│       ├── base.html              # ❌ 空文件（需要导航栏骨架）
│       ├── login.html             # ❌ 空文件
│       ├── register.html          # ❌ 空文件
│       ├── members.html           # ❌ 空文件
│       ├── stories.html           # ❌ 空文件
│       └── upload.html            # ❌ 空文件
├── static/
│   └── uploads/                   # 已建好
└── requirements.txt               # ❌ 未创建

## 进度总览

编号 | 步骤                      | 状态  | 备注
:--:|--------------------------|:----:|------
 1  | config.py                 | ✅   | ⚠️ 数据库名 students → timeprint
 2  | databases.py              | ✅   | ✅ 已补上 get_db() 公共函数
 3  | models/users.py           | ✅   |
 4  | models/member.py          | ✅   |
 5  | models/story.py           | ✅   |
 6  | services/auth.py          | ✅   |
 7  | routers/auth.py           | ✅   | ⚠️ bug：没导入 SessionLocal，会 NameError
 8  | routers/member.py         | ⚠️   | 只有列表查询，缺 add/edit/delete
 9  | routers/story.py          | ❌   | 空文件
10  | services/export.py        | ❌   | 空文件
11  | main.py                   | ❌   | 空文件 — 项目还跑不起来
12  | templates/*.html          | ❌   | 全部空文件
13  | Linux 部署                | ❌   | 代码完成后做

## 待修复的 Bug
1. **config.py** — 数据库连接 `students` → 改为 `timeprint`
2. **routers/auth.py** — 缺少 `from ..databases import SessionLocal` 导入
3. ~~**databases.py** — 建议加一个公共 `get_db` 函数~~ ✅ 已修复
4. **routers/member.py** — 导入 `from ..databases import get_db`，但 get_db 不在 databases 里

## 下一步建议
先修 bug（config + SessionLocal + get_db），然后写故事路由 → 模板 → main.py，让项目跑起来。
"""